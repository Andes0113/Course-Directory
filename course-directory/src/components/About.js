import React from 'react';

const About = (props) => (
  <div className="main-content">
    <h2>{ props.title }</h2>
    <p>This React.js based website consists of a directory of teachers at CSN's science department as well as a general course offerings page.</p>
  </div>
);

export default About;